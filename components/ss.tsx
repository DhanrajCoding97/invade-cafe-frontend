useGSAP( () => { if (!sectionRef.current) return; const tl = tlRef.current; tl.fromTo( eyebrowRef.current, { autoAlpha: 0, y: 20 }, { autoAlpha: 1, y: 0, duration: 0.5, ease: 'power4.inOut' }, 0, // starts at t=0 of the sequence ); // Cards, same shared timeline const cards = cardsRef.current?.children; if (cards) { gsap.set(cards, { autoAlpha: 0, y: 48 }); tl.to( cards, { autoAlpha: 1, y: 0, delay: 0.8, duration: 0.4, ease: 'power4.out', stagger: 0.3, }, '>+0.6', ); } // Single ScrollTrigger drives the whole timeline ScrollTrigger.create({ trigger: sectionRef.current, start: 'top 70%', once: true, onEnter: () => tl.play(), }); }, { scope: sectionRef }, );